using UnityEngine;

public class GameManager : MonoBehaviour
{
    [SerializeField] short pinesLeft;
    [SerializeField] short points;
    short pointsInThisRound;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
